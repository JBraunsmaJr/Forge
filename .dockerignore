# Build outputs
*.exe
dist/
# internal/scheduler/web/dist is intentionally NOT re-included: the image's
# UI must come from the uibuilder stage, never from ambient context state.
.forge/
node_modules/
**/node_modules/

# Version control
.git/
.gitignore

# Editor artifacts
.vscode/
.idea/
*.swp
*.swo
.venv

# Test artifacts
coverage.out
*.test

# Docker files (don't recurse)
Dockerfile*
docker-compose*.yml
.dockerignore